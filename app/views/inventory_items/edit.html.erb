<h1>Edit Inventory Item</h1>
<%= form_with(model: @inventory_item, data: { controller: "inventory-item" }) do |form| %>
  <div>
    <%= form.label :quantity %>
    <%= form.number_field :quantity %>
  </div>
  <%= form.submit 'Update' %>
<% end %>
